# Backend API URL (used when USE_MOCK_API is false)
VITE_API_BASE_URL=http://localhost:5001/api

# Toggle Mock API Mode
# true  = Use static demo data (no backend required)
# false = Use real backend API server
VITE_USE_MOCK_API=true
